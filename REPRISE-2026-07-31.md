# Point de reprise, 31 juillet 2026 : passage aux nouveaux modeles

**Etat : CODE TERMINE, COMMITE ET POUSSE (`c4b7c7c`) le 31/07 au matin.**
`npx tsc --noEmit` propre, 665 tests verts, `npm run build` vert.
Les etapes 1 a 4 sont faites. Il ne reste que les etapes 5 et 6, qui ne
sont pas du code : elles demandent un vrai import et un oeil sur la jauge.

Ce fichier reste la memoire du POURQUOI : si un import se met a mal lire un
tableau, ou si un message "reponse coupee" apparait, tout est explique ici.

---

## Pourquoi ce chantier

Deux changements de modele valides par Christophe le 31/07 :

1. **Imports (PDF/Word) : Sonnet 4.6 -> Sonnet 5.** Ce qui se joue est la
   LECTURE, pas le raisonnement : lire un PDF c'est de la vision, chaque page
   traitee comme une image. Sonnet 5 est le premier Sonnet a lire en haute
   resolution (2576 px sur le grand cote contre 1568), donc directement la
   finesse de dechiffrage des tableaux de Cecile. Opus reste ecarte : plus cher
   pour ameliorer un raisonnement qui n'est pas le goulot, et il depasse le
   temps max des fonctions serverless Vercel.
2. **Bilan et assistant : Sonnet 4.6 -> Haiku 4.5.** Trois fois moins cher
   (1 $/5 $ le million contre 3 $/15 $) pour ecrire un paragraphe court a
   partir d'un contexte deja mache.
3. **Le chat reste sur Sonnet 4.6.** Surface interactive, la latence s'y voit ;
   rien a gagner, un risque de lenteur a prendre. Decision deliberee.

### Le piege, et la vraie raison du garde-fou

Sur Sonnet 5, **ne pas passer de champ `thinking` n'eteint plus la reflexion,
ca l'active** : le defaut a change entre la 4.6 et la 5. Or `max_tokens`
plafonne la reflexion ET la reponse ENSEMBLE, et le modele ne voit pas ce
plafond, donc il ne se rationne pas.

Consequence concrete sur `ia-manuel` et `ia-edt` (tous deux a
`max_tokens: 16000`) : sur un tableau touffu le modele pourrait depenser
12 000 tokens a reflechir et rendre un JSON coupe au milieu d'un objet.
Aggravant : Sonnet 5 change de decoupage de tokens, le meme texte compte
environ 30 % de tokens en plus qu'en 4.6, donc un plafond calibre pour la 4.6
est deja plus serre qu'il n'en a l'air.

Et personne ne le verrait : aucune route ne verifiait `stop_reason`. Une
troncature ne remontait pas comme "reponse coupee" mais comme une SyntaxError
de `JSON.parse`, traduite par `messageErreurIA` en "Erreur IA, reessaie" : un
message FAUX qui envoie l'enseignante reessayer a l'identique, et repayer,
au lieu de raccourcir son document.

D'ou les deux volets : eteindre la reflexion explicitement sur les imports,
ET verifier `stop_reason` partout.

---

## FAIT (ne pas refaire)

### 1. `src/lib/ia/erreurs.ts`
Ajout de `messageReponseIncomplete(stopReason, conseil?)`. Renvoie `null` si la
reponse est complete, sinon `{ message, status: 422 }`. Couvre `max_tokens`
(reponse coupee) et `refusal` (statut 200, contenu vide ou partiel, memes
degats au parsing). Le message oriente vers "document plus court / page par
page", pas vers un simple "reessaie".

### 2. `src/lib/ia/__tests__/erreurs.test.ts`
6 tests ajoutes sur cette fonction (complet, null/undefined, max_tokens,
conseil personnalisable, refusal).

### 3. `src/lib/ia/anthropic.ts`
- `MODELE_IMPORT` passe a `'claude-sonnet-5'`
- `MODELE_CHAT` reste `'claude-sonnet-4-6'` (volontaire, voir plus haut)
- NOUVEAU `MODELE_COURT = 'claude-haiku-4-5'`
- NOUVEAU `REFLEXION_ETEINTE = { type: 'disabled' } as const`
Commentaires explicatifs ecrits dans le fichier.

### 4. Deja verifie
`src/lib/ia/cout.ts` couvre deja `claude-sonnet-5` et `claude-haiku-4-5` dans
`TARIFS_USD_PAR_MTOK`. Rien a faire cote jauge.

---

## FAIT LE 31/07 AU MATIN (etapes 1 a 4)

### Etape 1 : FAIT. `src/lib/ia/__tests__/anthropic.test.ts`
`describe('modeles')` ecrit : un `test.each` verifie que les trois modeles
reellement utilises (import, chat, court) sont au catalogue de tarifs, et un
test verrouille `REFLEXION_ETEINTE.type === 'disabled'`.

### Etape 2 : FAIT. Les 5 routes sont branchees
Detail ci-dessous, conserve comme reference de ce qui a ete change.
Toutes ont la meme forme : `client.messages.create(...)`, puis
`enregistrerUsageIA(...)`, puis `.content.find(b => b.type === 'text')` et
parsing. **Inserer le controle APRES `enregistrerUsageIA` et AVANT le
parsing** : les tokens produits sont factures meme tronques, la consommation
doit etre enregistree dans tous les cas.

Forme a inserer (adapter le conseil selon la route) :

```ts
const incomplete = messageReponseIncomplete(message.stop_reason)
if (incomplete) {
  return NextResponse.json({ error: incomplete.message }, { status: incomplete.status })
}
```

| Fichier | Modele | Autres changements |
|---|---|---|
| `src/app/api/ia-manuel/route.ts` | `MODELE_IMPORT` (inchange) | ajouter `thinking: REFLEXION_ETEINTE` dans le `create` ; **remplacer le commentaire "Pas de thinking" qui est devenu FAUX** (il dit que ne rien passer suffit) ; + controle `stop_reason` |
| `src/app/api/ia-edt/route.ts` | `MODELE_IMPORT` (inchange) | idem ia-manuel |
| `src/app/api/ia-bilan/route.ts` | `MODELE_CHAT` -> **`MODELE_COURT`** (2 endroits : `model:` ligne ~23 et `enregistrerUsageIA` ligne ~29) | + controle `stop_reason`, conseil adapte |
| `src/app/api/assistant/route.ts` | `MODELE_CHAT` -> **`MODELE_COURT`** (2 endroits : ligne ~58 et ~75) | + controle `stop_reason`, conseil du type "Pose une question plus courte." |
| `src/app/api/ia-chat/route.ts` | `MODELE_CHAT` (inchange) | + controle `stop_reason` uniquement |

Attention aux DEUX endroits par route : le champ `model:` de l'appel ET
l'argument `modele:` de `enregistrerUsageIA`. Les desynchroniser fausserait la
jauge de credit.

### Etape 3 : FAIT. Verification
`npx tsc --noEmit` propre (le SDK `^0.104.1` accepte bien la chaine
`'claude-sonnet-5'` et `thinking: { type: 'disabled' }`, la crainte etait
infondee), 665 tests verts sur 70 suites, `npm run build` vert avec les 5
routes d'API bien presentes.

### Etape 4 : FAIT. Commit + push
Deux commits sur `main` : `7074061` (couche basse, deja la) puis `c4b7c7c`
(les 5 routes). Pousses sur origin, donc deployes par Vercel.

---

## RESTE A FAIRE (pas du code)

### Etape 5 : verifier en vrai apres deploiement
Faire un import de PDF reel et confirmer que la lecture des tableaux est au
moins aussi bonne qu'avant. C'est le seul juge du gain de la haute resolution.

### Etape 6 : surveiller la consommation
La jauge doit montrer une baisse sur bilan et assistant (Haiku). Christophe
doit toujours saisir son releve reel de console.anthropic.com dans
Parametres > Credit IA, sinon la jauge n'affiche aucun solde (voir
`REPRISE-2026-07-30.md`).

---

## Si ca se passe mal

**Repli immediat** : remettre `MODELE_IMPORT = 'claude-sonnet-4-6'` dans
`src/lib/ia/anthropic.ts`. Le garde-fou `stop_reason` et le
`thinking: REFLEXION_ETEINTE` restent valables et sans effet de bord sur la
4.6 (ou ne rien passer et passer `disabled` reviennent au meme).

**Point de vigilance non verifie** : le SDK est en `^0.104.1`. La chaine
`'claude-sonnet-5'` devrait passer (le type `Model` accepte une chaine libre)
et `thinking: { type: 'disabled' }` existe depuis longtemps, mais **`tsc` n'a
pas encore ete lance**. Si ca coince, c'est la premiere piste : monter le SDK.

**Fenetre tarifaire** : Sonnet 5 est a tarif de lancement reduit jusqu'au
31/08/2026. `cout.ts` retient volontairement le tarif plein (3 $/15 $) :
surestimer est le defaut acceptable pour un compteur de budget.
