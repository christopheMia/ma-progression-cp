# Point de reprise, 30 juillet 2026

Deux chantiers menes le meme jour. Le premier est termine et pousse, le second
est a mi-parcours. Ce fichier suffit pour reprendre dans une nouvelle session.

---

## 1. Securite (TERMINE, deploye)

Commit `cc6e5ed`, en production, verifie en direct sur l'URL Vercel.

- Les cinq routes IA exigent la connexion (`garderAppelIA`, dans `src/lib/ia/garde.ts`).
  Le portier `src/proxy.ts` a un court-circuit de prechargement contournable ;
  chaque route verifie donc elle-meme l'identite.
- Next.js monte de 16.2.9 a 16.2.12 (contournement du proxy, divulgation des
  endpoints de server actions).
- Rapport complet et actions restantes : `partage/MARCHE-A-SUIVRE-SECURITE.md`.

---

## 2. Jauge de credit IA (COMMITE EN LOCAL, PAS DEPLOYE)

Commit `c37adb4`. **Ne pas pousser avant d'avoir applique la migration 024.**

### Le probleme

La jauge affichait 0,18 $ alors que la console Anthropic comptait 3,26 $
consommes sur 5 crediteds (1,66 $ restants). Facteur 18. Trois causes :

1. Les tokens de cache n'etaient pas comptes. L'API renvoie quatre compteurs et
   `input_tokens` ne contient que la part NON mise en cache ; l'assistant et le
   chat mettent leur prompt en cache.
2. `ia_usage.class_id` etait en `on delete cascade` : chaque reinitialisation de
   classe effacait l'historique pendant que la facture continuait.
3. Un appel passe avant la creation de la classe n'etait jamais enregistre.

### Ce qui est fait

- `src/lib/ia/cout.ts` : quatre compteurs, vrais multiplicateurs de cache
  (ecriture 1,25x, lecture 0,1x), tarif par modele, repli sur le tarif le plus
  cher pour un modele inconnu.
- `src/lib/actions/ia-usage.ts` : consommation rattachee a l'utilisateur ;
  fonction `soldeIA()` ancree sur un releve manuel ; echecs journalises.
- `src/components/parametres/CreditIaEditor.tsx` : saisie du solde reel.
- `garderAppelIA` renvoie 402 quand le credit estime est epuise.

### Fait structurant

L'application ne PEUT PAS lire le solde reel : l'API de couts d'Anthropic est
reservee aux comptes organisation, et le compte est individuel. D'ou l'ancrage
sur un releve manuel releve sur console.anthropic.com.

### A FAIRE EN PREMIER

1. Appliquer `supabase/migrations/024_ia_usage_fiable.sql` (editeur SQL Supabase,
   ou via MCP). **Le code ecrit dans des colonnes qui n'existent pas encore.**
2. Pousser, deployer.
3. Saisir 1,66 $ dans Parametres > Credit IA.

---

## 3. Import Word et Excel (EN COURS, non commite)

### Pourquoi

L'API n'accepte pas les formats binaires (.docx, .xlsx) : seuls le PDF et le
texte passent. Mais faire sortir une enseignante de l'application pour exporter
en PDF est une rupture de parcours, et le fichier Word ou Excel est souvent le
document de travail vivant, celui qu'elle modifie a chaque periode.

Contrairement au PDF, un .docx et un .xlsx contiennent leurs tableaux DEJA
structures : il n'y a aucune geometrie a reconstruire, contrairement a
`pdf-client.ts`. Le texte extrait est donc plus fidele que celui d'un PDF.

### Fait et teste (22 tests)

- `src/lib/ia/bureautique.ts` : parties pures, testables en Node.
  `lireChainesPartagees`, `lireFeuilleXlsx`, `grilleVersTexte`, `htmlVersTexte`,
  `colonneVersIndex`, `decoderEntites`.
- `src/lib/ia/bureautique-client.ts` : glue navigateur, chargement dynamique de
  `mammoth` (Word) et `fflate` (Excel), sur le modele de `pdf-client.ts`.
- `src/types/mammoth-browser.d.ts` : types de la surface utilisee.

### Choix de dependances (important)

Le paquet npm `xlsx` (SheetJS) porte deux failles hautes **sans correctif** :
ecarte. `exceljs` traine quatre dependances vulnerables et vise Node : ecarte.
Retenu : `mammoth` + `fflate`, qui n'ajoutent **aucune** vulnerabilite
(`npm audit` ne remonte plus que postcss et sharp, tous deux internes a Next).

### Reste a faire

1. Brancher dans `src/components/methodes/SourceImporter.tsx` : accepter
   `.docx,.xlsx` en plus de `.pdf`, appeler `extraireTexteBureautique` et
   poster le resultat dans le champ `texte` du FormData (ce chemin existe deja,
   ligne ~416).
2. Brancher dans `src/components/parametres/ImporterEdtButton.tsx`, ce qui
   demande d'ajouter un chemin texte a `src/app/api/ia-edt/route.ts` : cette
   route n'accepte aujourd'hui que des PDF. Prevoir une variante de
   `SYSTEM_IMPORT_EDT` (il dit « fourni en PDF »).
3. Message d'aide pour les formats anciens (.doc, .xls) et les documents dont
   la progression est une image : `estFormatAncien` est deja ecrit.
4. Tests des deux composants, puis build.

---

## Modele : recommandation en attente de decision

Actuellement `claude-sonnet-4-6` partout (`src/lib/ia/anthropic.ts`).

- **Imports PDF : passer a `claude-sonnet-5`.** Premier Sonnet a lire les images
  en haute resolution (2576 px contre 1568). La doc precise que la lecture de
  PDF repose sur la vision : chaque page est traitee comme une image. C'est donc
  directement la qualite de lecture des tableaux de Cecile qui est en jeu.
  Moins cher que la 4.6 jusqu'au 31/08 (tarif de lancement).
- **Bilan et assistant : passer a `claude-haiku-4-5`.** Trois fois moins cher,
  suffisant pour un paragraphe court.
- **Pas Opus** : 1,7 fois le prix pour un goulot qui est la lecture de document,
  pas le raisonnement.

Attention : sur Sonnet 5 la reflexion adaptative est active par defaut alors
qu'elle etait eteinte sur la 4.6, et `max_tokens` plafonne reflexion et reponse
ensemble. Il faudra l'eteindre explicitement, sinon un gros import sera tronque.
La table de tarifs de `cout.ts` couvre deja sonnet-5 et haiku-4-5.
